dist: trusty
os: linux

sudo: required

services:
  - docker
        
cache:
    directories:
      - $PWD/mignon_test_data

language: java
jdk: openjdk8

## travis build will check wether the WDL code behaves correctly regarding the inputs/outputs of the different inputs of the workflow.
script: bash $PWD/runMignonExample.sh
